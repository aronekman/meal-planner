{
  "type": "module",
  "scripts": {
    "gcp-build": "cd api && npm ci && npm run build && cd ../client && npm ci && npm run build && move dist ../api/build",
    "start": "node api/build/index.js"
  }
}
